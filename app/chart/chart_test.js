'use strict';

describe('myApp.chart module', function() {

  beforeEach(module('myApp.chart'));

  describe('chart controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var chartCtrl = $controller('ChartCtrl');
      expect(chartCtrl).toBeDefined();
    }));

  });
});